import { useState } from "react";
import ReactDOM from "react-dom/client";
import RandomNumber from "./Random";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(<div>
<RandomNumber></RandomNumber></div>
)